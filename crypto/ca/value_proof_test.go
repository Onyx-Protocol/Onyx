package ca

import "testing"

func TestValueProof(t *testing.T) {
	// assetID := AssetID{1}
	// value := uint64(2)
	// aek := AssetKey{3}
	// ac, c := CreateAssetCommitment(assetID, aek)
	// vek := ValueKey{4}
	// vc, f := CreateValueCommitment(value, ac, vek)
	// msg := []byte("message")
	// vp := CreateValueProof(value, *c, *f, msg)
	// if !vp.Validate(vc, assetID, value, msg) {
	// 	t.Error("failed to validate value proof")
	// }
	// if vp.Validate(vc, AssetID{1, 1}, value, msg) {
	// 	t.Error("validated invalid value proof")
	// }
	// if vp.Validate(vc, assetID, value+1, msg) {
	// 	t.Error("validated invalid value proof")
	// }
	// if vp.Validate(vc, assetID, value, msg[1:]) {
	// 	t.Error("validated invalid value proof")
	// }
}
