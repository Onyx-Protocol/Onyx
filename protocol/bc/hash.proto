syntax = "proto3";

package bc;

// Warning: we generate an empty placeholder message type here
// so that other messages in this package can refer to it.
// However, we do not generate code for this type;
// we provide our own implementation.
// See our hand-written hash.go.

message Hash    {}
message AssetID {}
