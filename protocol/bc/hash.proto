syntax = "proto3";

package bc;

// Warning: we generate an empty placeholder message type here
// so that other messages in this package can refer to it.
// However, we do not use the generated code for this type;
// we discard it and provide our own implementation.
// File hash.pb.go is removed; see our hand-written hash.go.

message Hash    {}
message AssetID {}
